AMBER_API_KEY=${AMBER_API_KEY}
DATABASE_URL=${DATABASE_URL}

# Historical Data Start Date (REQUIRED)
# Specify the date to start collecting historical data from
HISTORICAL_START_DATE=${HISTORICAL_START_DATE}

# Data Collection Configuration
# You can use environment variables with defaults
COLLECTION_INTERVAL_MINUTES=${COLLECTION_INTERVAL_MINUTES}

# Logging and Environment
LOG_LEVEL=${LOG_LEVEL}
ENVIRONMENT=${ENVIRONMENT}